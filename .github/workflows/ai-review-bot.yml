name: AI Review Bot

on:
  pull_request:
    types: [opened]

permissions:
  contents: read
  pull-requests: write

jobs:
  ai-review-bot:
    uses: unstoppabledomains/agent-plugins/.github/workflows/ai-review-bot-reusable.yml@main
    secrets: inherit
